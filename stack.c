#include "monty.h"

/**
 * _stack - prints the top
 * @head: stack head
 * @line: line_number
 * Return: no return
*/

void _stack(stack_t **top, unsigned int line)
{
	(void)top;
	(void)line;
	mon.flag = 0;
}
