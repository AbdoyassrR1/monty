#include "monty.h"

/**
 * _stack - prints the top
 * @top: stack head
 * @line: line_number
 * Return: no return
*/

void _stack(stack_t **top, unsigned int line)
{
	(void)top;
	(void)line;
	mon.fl = 0;
}
